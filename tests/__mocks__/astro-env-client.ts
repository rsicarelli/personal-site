// Test stub for the `astro:env/client` virtual module, which only exists inside an Astro build.
// src/config/site.ts imports PUBLIC_SITE_URL from it; the tests don't depend on the real value.
export const PUBLIC_SITE_URL = 'https://rsicarelli.com';
